Global.Element = function(config) {
    'use strict';

    var Mine = Global.Element,
        instanse = {
            check_all: $('#all'),
            btn_create: $('#create'),
            srcs: $('#srcfile input'),
            srcarea: $('#srcarea')
        };

    return instanse;
};
