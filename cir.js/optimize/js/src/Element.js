Global.Element = function(config) {
    'use strict';

    var Mine = Global.Element,
        $ = C.$,
        instanse = {
            check_all: $('#all'),
            btn_create: $('#create'),
            srcs: $('#srcfile input'),
            srcarea: $('#srcarea')
        };

    return instanse;
};
