Global.Element = function(config) {
    'use strict';

    var Mine = Global.Element,
        instanse = {
            check_all: $('#all'),
            btn_create: $('#create'),
            srcs: $('#srcfile li input'),
            srcarea: $('#srcarea')
        };

    return instanse;
};
